# East Fu Lean APS 使用手冊

## 1. 系統用途

東福精實生產排程系統用來把 Excel 工單、產品機台產速、換模群組與現場限制轉成可執行的排程。系統定位是小型工廠現場可用的排程輔助工具，不取代現場判斷。

## 2. Excel 準備

Excel 至少需要兩張工作表：

- `待排工單`
- `產品機台產速`

建議一併填寫：

- `機台初始狀態`
- `換模時間`

新版範本會多一張 `填表說明`，用來解釋每個欄位。必填欄位用紅色系底色標示。

### 待排工單

| 欄位 | 必填 | 說明 | 範例 |
|---|---|---|---|
| 工單編號 | 是 | 工單或製令唯一編號，不可重複。 | WO-20260905-001 |
| 產品 | 是 | 要生產的產品代號，必須能在產品機台產速表找到。 | SIM-C2-A |
| 數量 | 是 | 本工單要生產的數量，必須大於 0。 | 600 |
| 單位 | 是 | 數量單位，目前以 PCS/hr 換算加工時間。 | PCS |
| 優先級 | 是 | 支援急單、一般、低優先。 | 急單 |
| 交期 | 是 | 工單需要完成的時間。只填日期時視為當天 23:59:59。 | 2026/09/05 18:00 |
| 工單限定機台 | 否 | 單張工單例外限制。空白代表依產品機台產速表判斷。 | C2,C5 |

### 產品機台產速

| 欄位 | 必填 | 說明 | 範例 |
|---|---|---|---|
| 產品 | 是 | 產品代號。 | SIM-C2-A |
| 機台 | 是 | 可生產該產品的機台。 | C2 |
| 產速_PCS_per_hr | 是 | 每小時可生產 PCS 數。 | 120 |
| 換模群組 | 是 | 產品族群或模具/換線分類。 | C2-A |

同一產品若可在多台機台生產，請拆成多列，例如：

| 產品 | 機台 | 產速_PCS_per_hr | 換模群組 |
|---|---|---:|---|
| A產品 | C2 | 120 | A |
| A產品 | C4 | 100 | A |

### 機台初始狀態

| 欄位 | 必填 | 說明 | 範例 |
|---|---|---|---|
| 機台 | 是 | 排程開始前的機台。 | C2 |
| 初始產品 | 是 | 排程開始前該機台正在做或最後做的產品。 | SIM-C2-A |

這張表用來判斷第一張工單是否需要換模。若不確定，可先填目前最接近的產品。

### 換模時間

| 欄位 | 必填 | 說明 | 範例 |
|---|---|---|---|
| 來源換模群組 | 是 | 上一張工單的換模群組。 | C2-A |
| 目標換模群組 | 是 | 下一張工單的換模群組。 | C2-B |
| 換模時間_分鐘 | 是 | 從來源群組切換到目標群組需要的分鐘數。 | 20 |

排程規則：

1. 同一換模群組接續生產，換模時間為 0。
2. 換模時間表有設定來源與目標群組時，使用該分鐘數。
3. 查不到群組組合時，使用畫面上的預設換模時間。

## 3. 畫面操作

1. 上傳 Excel，或按 `載入東福標準排程資料`。
2. 確認資料驗證通過。
3. 設定排程期間與排程開始時間。
4. 選擇排程策略。
5. 選擇本次排程使用機台。
6. 在基本設定確認工時模式、人力/產能狀況、換模預設時間、週末與休假日。
7. 按 `確認設定並開始排程`。

## 4. 特殊狀況 / 停機

臨時停機、保養、盤點、設備不可用都在 `特殊狀況 / 停機` 輸入。

需要填：

- 機台
- 不可用開始日期與時間
- 不可用結束日期與時間
- 原因

系統會避開該時段，不會把工單排進去。若填錯，可按 `清除特殊狀況`。

## 5. 急單

急單區可以臨時新增一張工單並重新排程。急單不會覆蓋原本 Excel，而是以本次畫面狀態建立新的排程版本。

## 6. 工時模式

`24 小時連續排程`：排程期間內連續可排，除非遇到週末、指定休假日或特殊狀況 / 停機。

`每日 8 小時排程`：依每日開始時間與每日可排工時自動避開非工作時間。工單不拆分，不跨不可用時段。

## 7. 人力 / 產能狀況

若當天人力不足，可選擇：

- 正常產能（100%）
- 人力不足（80%）
- 嚴重不足（60%）
- 自訂

系統會用折減後的產速排程。

## 8. KPI 解讀

| KPI | 定義 |
|---|---|
| 準時率 | 未遲交工單數 / 總工單數。 |
| 遲交工單 | 完工時間晚於交期的工單張數。 |
| 未排入 | 無法在本次排程期間完成的工單數。 |
| 換模次數 | 換模時間大於 0 的次數。 |
| 總遲交 | 所有工單 max(完工時間 - 交期, 0) 的小時數加總。 |
| 最大遲交 | 單張工單最大遲交小時數。 |
| Makespan | 最後一張已排入工單的完工時間 - 本次排程開始時間。 |
| 平均使用率 | 各機台加工時間 / 排程期間小時數，再取 C2/C4/C5 平均；目前不含換模時間。 |

## 9. 歷史排程

只有成功完成排程或重排後，系統才建立正式歷史版本。單純上傳 Excel、選檔、修改參數，不會建立版本。

歷史版本保存當時的訂單、排程期間、排程規則、產速、換模設定、特殊狀況、甘特圖資料與 KPI。

## 10. 常見問題

### 為什麼全部遲交？

先確認交期是否早於排程開始時間。若 Excel 只填日期，系統會視為當天 23:59:59；若需要上午或下午交貨，請填日期加時間。

### 為什麼工單排不進去？

常見原因是產品沒有產速、本次使用機台未勾選該產品可用機台、工單限定機台後沒有合格機台、排程期間太短，或停機/休假時間太多。

### 工單限定機台和產品機台產速有什麼不同？

產品機台產速是產品層級的基本能力。工單限定機台是單張工單的例外限制。

### 換模群組是什麼？

換模群組是產品族群或模具/換線分類。群組不同時，系統會依換模時間表加上換模時間。
